import React from 'react';

import {Link} from 'react-router-dom';

const Cart = (props) => {
    return (
        <div>
            <Link to="/"> Home </Link>
        </div>
    );
};

export default Cart;